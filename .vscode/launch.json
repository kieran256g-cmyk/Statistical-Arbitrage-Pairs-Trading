{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Choose companies and strategy",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/run.py",
      "args": ["--choose"],
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}
